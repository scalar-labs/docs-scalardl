# This is a configuration file for the Gemini code review tool. It disables Gemini auto code review for the entire repository by ignoring all files and turning off code reviews.

have_fun: false
ignore_patterns:
  - "**/*"
code_review:
  disable: true
